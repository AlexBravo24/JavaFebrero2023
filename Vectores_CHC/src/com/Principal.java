package com;

import java.util.Vector;

//VECTOR
/*Es una estructura de datos que permite 
 * almacenar un CONJUNTO de datos del mismo tipo
 * Con un único nombre se define vector y por medio de un
 * subíndica hacemos referencia a cada elemento del
 * mismo (componente)
 * 
 */

//import java.util; //Cuando creamos un objeto de la 
//clase vector podemos especificar su dimensión.
public class Principal {

	
	Vector vector = new Vector(20,5);
	//Vector con dimensión inicial de 20 elementos, si rebasamos
	//dicha dimensión el vector crece a 25
	
//	Vector vector = new Vector(20);
//	
//	Vector vector=new Vector ();
//	v.addElement("uno");
//    v.addElement("dos");
//    v.addElement("cuatro");
//    v.addElement("cinco");
//	
	
}
